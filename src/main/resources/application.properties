server.port=8086
spring.data.rest.base-path=/api
spring.datasource.url = jdbc:mysql://localhost:3306/dmdb?serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = Wsy13759513007
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.http.multipart.maxFileSize=200Mb
spring.http.multipart.maxRequestSize=200Mb

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#AWS S3 values
aws.access.key.id = "AKIAQU3EF53UL4RJLRQU"
aws.access.key.secret = "7ekEfJIe+wRp0A502UQ6zWjBzrPr0LC8ed+F+hi3"
aws.region = your-aws-service-region
aws.s3.audio.bucket = your-bucket-name

##JWT token
application.jwt.secret=securitynUmweriojdf234vnkjnksdfldjiKSKSK
application.jwt.tokenPrefix=Bearer 
application.jwt.tokenExpirationAfterDays=10